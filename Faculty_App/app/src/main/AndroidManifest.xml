<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faculty_App"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".data.views.UserListActivity" />

        <activity android:name=".data.views.TeacherListActivity" />

        <activity android:name=".data.views.DepartmentListActivity" />

        <activity android:name=".data.views.StudentListActivity" />

        <activity android:name=".ui.users.EditUserActivity" />

        <activity android:name=".ui.teachers.EditTeacherActivity" />

        <activity android:name=".ui.dapartments.EditDepartmentActivity" />

        <activity android:name=".ui.users.AddUserActivity"/>

        <activity android:name=".ui.teachers.AddTeacherActivity"/>

        <activity android:name=".ui.dapartments.AddDepartmentActivity"/>

        <activity android:name=".data.views.DirectionListActivity" />

        <activity android:name=".data.views.GroupListActivity" />

        <activity android:name=".data.views.UserToGroupListActivity" />

        <activity android:name=".ui.user_to_group.EditUserToGroupActivity" />

        <activity android:name=".ui.groups.EditGroupActivity" />

        <activity android:name=".ui.directions.EditDirectionActivity" />

        <activity android:name=".ui.user_to_group.AddUserToGroupActivity"/>

        <activity android:name=".ui.groups.AddGroupActivity"/>

        <activity android:name=".ui.directions.AddDirectionActivity"/>

        <activity android:name=".ui.filter.FilterActivity" />

    </application>

</manifest>